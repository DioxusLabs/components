The button element is used to trigger actions or events in a user interface.

## Component Structure

```rust
button {
    // Global html attributes
    class: "button",
    "data-style": "outline",
    // Children
    {children}
}
```
